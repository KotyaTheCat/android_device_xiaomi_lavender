allow sensors unlabeled:dir { search } ;
allow sensors unlabeled:dir { getattr } ;
allow sensors unlabeled:dir { open } ;
allow sensors unlabeled:dir { read } ;
allow sensors unlabeled:file { getattr } ;
allow sensors unlabeled:file { open } ;
allow sensors unlabeled:file { read } ;
allow sensors unlabeled:file { read write } ;
